'use strict';

/*
let str = "Вот, что мне хотелось бы сказать на эту тему:";
let maxLength = 20;

function truncate(a, b) {

    if (a.length > b) {
        console.log(str.slice(0,b - 1) + "...")
    }
}

truncate(str, maxLength);*/

// let userData = prompt("Введи год рождения", "200-08-14")
/*
let userData = "200-08-14";
let birthday = new Date(userData) === "Invalid Date" ? new Date() : new Date(userData);
console.log(birthday);
let days = ['воскресенье', 'понедельник', 'вторник', 'среда', 'четверг', 'пятница', 'суббота'];
alert("Ваш день роджения: " + days[birthday.getDay()]);
*/
